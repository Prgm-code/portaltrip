meta {
  name: Healthcheck
  type: http
  seq: 1
  tags: [
    smoke
  ]
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

assert {
  res.status: eq 200
  res.body.status: eq UP
}
