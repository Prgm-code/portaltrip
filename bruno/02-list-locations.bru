meta {
  name: List locations
  type: http
  seq: 2
  tags: [
    smoke
  ]
}

get {
  url: {{baseUrl}}/api/v1/locations
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

assert {
  res.status: eq 200
  res.body.status: eq 200
  res.body.data: isArray
}
